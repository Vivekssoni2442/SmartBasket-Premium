<x-seller-verification.layout><h2 class="h4">Step 1: Email Verification</h2><p>Enter your registered Seller Email ID. A secure 16-digit code will be sent only to that address.</p><form method="post" action="{{ route('seller.verification.email.send') }}">@csrf<input class="form-control mb-2" type="email" name="email" value="{{ old('email', $seller->email) }}" required><button class="btn btn-primary">Send verification code</button></form><hr><form method="post" action="{{ route('seller.verification.email.verify') }}">@csrf<label class="form-label">16-digit verification code</label><input class="form-control mb-2" inputmode="numeric" pattern="[0-9]{16}" maxlength="16" name="code" required><button class="btn btn-success">Verify email</button></form></x-seller-verification.layout>

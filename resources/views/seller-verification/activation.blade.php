<x-seller-verification.layout><h2 class="h4">Step 6: Seller activation</h2><p>Enter the 16-digit activation code sent after admin approval. The code expires in 30 minutes.</p><form method="post" action="{{ route('seller.activation.verify') }}">@csrf<input class="form-control mb-3" name="code" inputmode="numeric" autocomplete="one-time-code" maxlength="16" pattern="[0-9]{16}" required><button class="btn btn-success">Activate seller account</button></form><form class="mt-3" method="post" action="{{ route('seller.activation.resend') }}">@csrf<button class="btn btn-link p-0">Resend activation code</button></form></x-seller-verification.layout>

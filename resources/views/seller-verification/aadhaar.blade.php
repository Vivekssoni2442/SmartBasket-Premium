<x-seller-verification.layout><h2 class="h4">Step 3: Aadhaar Verification</h2>@if(! $configured)<div class="alert alert-warning">Aadhaar verification service is not configured yet.</div>@else<p>Verification is completed through the authorized provider. SmartBasket does not retain your Aadhaar number or OTP.</p><form method="post" action="{{ route('seller.verification.aadhaar.start') }}">@csrf<input class="form-control mb-2" name="aadhaar_identifier" autocomplete="off" required><button class="btn btn-primary">Start authorized verification</button></form><hr><form method="post" action="{{ route('seller.verification.aadhaar.verify') }}">@csrf<input class="form-control mb-2" name="otp" inputmode="numeric" autocomplete="one-time-code" required><button class="btn btn-success">Verify provider OTP</button></form>@endif</x-seller-verification.layout>

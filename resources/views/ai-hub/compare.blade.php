<!DOCTYPE html><html lang="en"><head>@include('ai-hub.partials.head', ['title' => 'Compare Products'])</head><body><div class="ai-hub-layout">@include('ai-hub.partials.navigation')<main class="ai-hub-main"><header class="ai-hub-heading"><div><span class="ai-hub-eyebrow">Make confident choices</span><h1>Compare Products</h1><p>Select two products for a clear side-by-side comparison.</p></div></header><form class="ai-panel row g-3 mb-4" method="GET" action="{{ route('compare-products') }}"><div class="col-md-5"><label class="form-label">First product</label><select class="form-select" name="product_one"><option value="">Select product</option>@foreach($products as $product)<option value="{{ $product->id }}" {{ optional($firstProduct)->id === $product->id ? 'selected' : '' }}>{{ $product->name }}</option>@endforeach</select></div><div class="col-md-5"><label class="form-label">Second product</label><select class="form-select" name="product_two"><option value="">Select product</option>@foreach($products as $product)<option value="{{ $product->id }}" {{ optional($secondProduct)->id === $product->id ? 'selected' : '' }}>{{ $product->name }}</option>@endforeach</select></div><div class="col-md-2 d-flex align-items-end"><button class="btn btn-primary w-100">Compare</button></div></form>@if($firstProduct && $secondProduct)<div class="ai-panel table-responsive"><table class="table ai-compare-table align-middle"><thead><tr><th>Feature</th><th>{{ $firstProduct->name }}</th><th>{{ $secondProduct->name }}</th></tr></thead><tbody><tr><th>Image</th><td><img src="{{ asset('products/' . $firstProduct->image) }}" alt="{{ $firstProduct->name }}"></td><td><img src="{{ asset('products/' . $secondProduct->image) }}" alt="{{ $secondProduct->name }}"></td></tr><tr><th>Price</th><td>₹{{ number_format((float) $firstProduct->price, 2) }}</td><td>₹{{ number_format((float) $secondProduct->price, 2) }}</td></tr><tr><th>Rating</th><td>{{ number_format((float) ($firstProduct->rating ?? 0), 1) }} ★</td><td>{{ number_format((float) ($secondProduct->rating ?? 0), 1) }} ★</td></tr><tr><th>Category</th><td>{{ $firstProduct->category ?: '—' }}</td><td>{{ $secondProduct->category ?: '—' }}</td></tr><tr><th>Features</th><td>{{ $firstProduct->description ?: $firstProduct->brand ?: '—' }}</td><td>{{ $secondProduct->description ?: $secondProduct->brand ?: '—' }}</td></tr></tbody></table></div>@endif</main></div></body></html>

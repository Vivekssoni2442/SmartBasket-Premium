<!DOCTYPE html><html lang="en"><head>@include('ai-hub.partials.head', ['title' => 'Wishlist'])</head><body><div class="ai-hub-layout">@include('ai-hub.partials.navigation')<main class="ai-hub-main"><header class="ai-hub-heading"><div><span class="ai-hub-eyebrow">Saved for later</span><h1>Your Wishlist</h1><p>Your favourite products, ready to move into your existing cart.</p></div></header>@if(session('success'))<div class="alert alert-success">{{ session('success') }}</div>@endif<div class="ai-panel">@if($wishlistItems->isNotEmpty())<div class="ai-product-grid">@foreach($wishlistItems as $wishlistItem) @if($wishlistItem->product)<article class="ai-product-card"><img src="{{ asset('products/' . $wishlistItem->product->image) }}" alt="{{ $wishlistItem->product->name }}"><div class="ai-product-card-body"><h3>{{ $wishlistItem->product->name }}</h3><p>{{ $wishlistItem->product->category }}</p><div class="ai-price mb-3">₹{{ number_format((float) $wishlistItem->product->price, 2) }}</div><div class="d-flex gap-2"><form method="POST" action="{{ route('cart.add', $wishlistItem->product->id) }}" class="flex-grow-1">@csrf<button class="btn btn-primary btn-sm w-100">Add to cart</button></form><form method="POST" action="{{ route('wishlist.remove', $wishlistItem) }}">@csrf @method('DELETE')<button class="btn btn-outline-danger btn-sm" aria-label="Remove from wishlist"><i class="fa-solid fa-trash"></i></button></form></div></div></article>@endif @endforeach</div>@else<div class="ai-empty"><i class="fa-regular fa-heart"></i>Your wishlist is empty. Save products from the catalog to see them here.</div>@endif</div></main></div></body></html>

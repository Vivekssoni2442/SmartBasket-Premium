<!DOCTYPE html><html lang="en"><head>@include('ai-hub.partials.head', ['title' => 'AI Camera Assistant'])</head><body><div class="ai-hub-layout">@include('ai-hub.partials.navigation')<main class="ai-hub-main"><header class="ai-hub-heading"><div><span class="ai-hub-eyebrow">Future-ready AI</span><h1>AI Camera Assistant</h1><p>Upload or capture a product image to prepare recommendations.</p></div></header>@if(session('success'))<div class="alert alert-success">{{ session('success') }}</div>@endif<div class="row g-4"><div class="col-lg-5"><form class="ai-panel" method="POST" action="{{ route('ai-camera.analyze') }}" enctype="multipart/form-data">@csrf<label class="ai-upload-zone" for="product_image"><span><i class="fa-solid fa-camera"></i><strong class="d-block mt-3">Capture or upload a product image</strong><small class="text-muted">JPG, PNG or WEBP · up to 5 MB</small></span><img id="imagePreview" class="ai-preview" alt="Selected product preview"></label><input id="product_image" name="product_image" type="file" accept="image/*" capture="environment" class="d-none" required><button class="btn btn-primary w-100 mt-3" type="submit"><i class="fa-solid fa-wand-magic-sparkles me-1"></i> Generate recommendations</button></form></div><div class="col-lg-7"><div class="ai-panel h-100"><h2 class="h4 mb-3">Recommendation area</h2>@if($recommendations->isNotEmpty())<div class="ai-product-grid">@foreach($recommendations as $product) @include('ai-hub.partials.product-card') @endforeach</div>@else<div class="ai-empty"><i class="fa-solid fa-sparkles"></i>Select an image to begin. Your image is not saved by Smart Basket.</div>@endif</div></div></div></main></div><script>document.getElementById('product_image').addEventListener('change', function () { const preview=document.getElementById('imagePreview'); if(this.files[0]) { preview.src=URL.createObjectURL(this.files[0]); preview.style.display='block'; preview.previousElementSibling.style.display='none'; } });</script></body></html>

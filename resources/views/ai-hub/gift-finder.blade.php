<!DOCTYPE html><html lang="en"><head>@include('ai-hub.partials.head', ['title' => 'Gift Finder'])</head><body><div class="ai-hub-layout">@include('ai-hub.partials.navigation')<main class="ai-hub-main"><header class="ai-hub-heading"><div><span class="ai-hub-eyebrow">Thoughtful gifting</span><h1>Gift Finder</h1><p>Choose an occasion for a selection of gift-ready recommendations.</p></div></header><div class="ai-panel mb-4"><form method="GET" action="{{ route('gift-finder') }}" class="d-flex flex-wrap gap-2">@foreach(['Birthday', 'Anniversary', 'Festival'] as $item)<button class="btn ai-occasion-btn {{ $occasion === $item ? 'btn-primary' : 'btn-outline-primary' }}" name="occasion" value="{{ $item }}">{{ $item }}</button>@endforeach</form></div>@if($occasion)<div class="ai-panel">@if($products->isNotEmpty())<p class="text-muted mb-4">Recommended for {{ $occasion }}.</p><div class="ai-product-grid">@foreach($products as $product) @include('ai-hub.partials.product-card') @endforeach</div>@else<div class="ai-empty"><i class="fa-solid fa-gift"></i>No gift picks are available yet.</div>@endif</div>@endif</main></div></body></html>

<div class="ai-panel-fragment"><h2>🎁 Gift Finder</h2><p>Select an occasion for curated ideas.</p><div class="d-grid gap-2">@foreach(['Birthday', 'Anniversary', 'Festival'] as $item)<a class="btn {{ $occasion === $item ? 'btn-primary' : 'btn-outline-primary' }} btn-sm" href="{{ route('gift-finder', ['occasion' => $item, 'sidebar' => 1]) }}" data-ai-hub-panel-link>{{ $item }}</a>@endforeach</div>@if($occasion)<div class="ai-drawer-products">@forelse($products as $product)<a href="{{ route('products.show', $product->id) }}"><span>{{ $product->name }}</span><b>₹{{ number_format((float) $product->price, 2) }}</b></a>@empty<p class="mb-0">No recommendations yet.</p>@endforelse</div>@endif</div>

<div class="ai-panel-fragment"><h2>🌟 Trending Products</h2><p>Popular products ranked by available sales and ratings.</p><div class="ai-drawer-products">@forelse($trendingProducts->take(8) as $product)<a href="{{ route('products.show', $product->id) }}"><span>{{ $product->name }}<small>{{ $sales[$product->id] ?? 0 }} sold · ★ {{ number_format((float) ($product->rating ?? 0), 1) }}</small></span><b>₹{{ number_format((float) $product->price, 2) }}</b></a>@empty<p class="mb-0">No products available yet.</p>@endforelse</div></div>

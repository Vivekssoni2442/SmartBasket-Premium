<div class="ai-panel-fragment"><h2>📷 AI Camera</h2><p>Capture or upload an image to prepare product suggestions.</p><form action="{{ route('ai-camera.analyze') }}" method="POST" enctype="multipart/form-data" data-ai-hub-form>@csrf<input type="file" name="product_image" accept="image/*" capture="environment" class="form-control mb-2" required><button class="btn btn-primary btn-sm w-100">Generate suggestions</button></form>@if($recommendations->isNotEmpty())<div class="ai-drawer-products">@foreach($recommendations as $product)<a href="{{ route('products.show', $product->id) }}"><span>{{ $product->name }}</span><b>₹{{ number_format((float) $product->price, 2) }}</b></a>@endforeach</div>@endif</div>

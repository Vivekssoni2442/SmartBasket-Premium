<div class="ai-panel-fragment"><h2>💰 Budget Shopping</h2><p>Find products within your budget.</p><form action="{{ route('budget-shopping') }}" method="GET" data-ai-hub-form><input type="number" name="budget" min="0" step="0.01" value="{{ $budget }}" class="form-control mb-2" placeholder="Enter amount, e.g. 500" required><button class="btn btn-primary btn-sm w-100">Find products</button></form>@if($budget !== null && $budget !== '')<div class="ai-drawer-products">@forelse($products as $product)<a href="{{ route('products.show', $product->id) }}"><span>{{ $product->name }}</span><b>₹{{ number_format((float) $product->price, 2) }}</b></a>@empty<p class="mb-0">No matching products found.</p>@endforelse</div>@endif</div>

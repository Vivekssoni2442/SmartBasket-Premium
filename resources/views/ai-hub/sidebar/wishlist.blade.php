<div class="ai-panel-fragment"><h2>❤️ Wishlist</h2><p>Your saved favourites.</p>@if($wishlistItems->isNotEmpty())<div class="ai-drawer-products">@foreach($wishlistItems as $item)@if($item->product)<a href="{{ route('products.show', $item->product->id) }}"><span>{{ $item->product->name }}</span><b>₹{{ number_format((float) $item->product->price, 2) }}</b></a>@endif @endforeach</div>@else<p class="mb-0">Your wishlist is empty.</p>@endif</div>

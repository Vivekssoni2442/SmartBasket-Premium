<!DOCTYPE html><html lang="en"><head>@include('ai-hub.partials.head', ['title' => 'Budget Shopping'])</head><body><div class="ai-hub-layout">@include('ai-hub.partials.navigation')<main class="ai-hub-main"><header class="ai-hub-heading"><div><span class="ai-hub-eyebrow">Shop your way</span><h1>Budget Shopping</h1><p>Enter your budget and discover products priced within it.</p></div></header><form class="ai-panel row g-3 mb-4" method="GET" action="{{ route('budget-shopping') }}"><div class="col-md-8"><label class="form-label">Your budget (₹)</label><input class="form-control" type="number" name="budget" min="0" step="0.01" value="{{ $budget }}" placeholder="Example: 500" required></div><div class="col-md-4 d-flex align-items-end"><button class="btn btn-primary w-100">Find products</button></div></form>@if($budget !== null && $budget !== '')<div class="ai-panel">@if($products->isNotEmpty())<p class="text-muted mb-4">{{ $products->count() }} products found within ₹{{ number_format((float) $budget, 2) }}.</p><div class="ai-product-grid">@foreach($products as $product) @include('ai-hub.partials.product-card') @endforeach</div>@else<div class="ai-empty"><i class="fa-solid fa-wallet"></i>No products currently match this budget.</div>@endif</div>@endif</main></div></body></html>

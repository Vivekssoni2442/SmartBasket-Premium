@extends('layouts.admin')
@section('title','Products')
@section('content')
<div class="admin-page-header"><div><h1 class="admin-page-title"><i class="fas fa-cubes"></i>Products</h1><p class="admin-page-description">Live product catalogue moderation and inventory overview.</p></div></div><form class="admin-toolbar" method="GET"><input class="admin-input" name="search" value="{{ request('search') }}" placeholder="Product name"><select class="admin-select" name="status"><option value="">All statuses</option><option value="active" @selected(request('status')==='active')>Active</option><option value="inactive" @selected(request('status')==='inactive')>Inactive</option></select><button class="admin-btn admin-btn-primary">Filter</button></form><div class="admin-card"><div class="admin-table-wrap"><table class="admin-table"><thead><tr><th>Product</th><th>Seller</th><th>Category</th><th>Price</th><th>Stock</th><th>Status</th><th></th></tr></thead><tbody>@forelse($products as $product)<tr><td><span style="display:inline-grid;place-items:center;width:38px;height:38px;background:#0b1627;border-radius:10px;margin-right:8px;vertical-align:middle;overflow:hidden">@if($product->image)<img src="{{ asset('products/'.$product->image) }}" alt="" style="width:100%;height:100%;object-fit:cover">@else<i class="fas fa-image" style="color:var(--admin-muted)"></i>@endif</span>{{ $product->name }}</td><td>{{ $product->seller?->business_name ?: $product->seller?->name ?: 'Platform' }}</td><td>{{ $product->category }}</td><td>₹{{ number_format($product->price,2) }}</td><td>{{ $product->stock }}</td><td><span class="admin-badge admin-badge-{{ ($product->status ?? 'active')==='active'?'success':'neutral' }}">{{ $product->status ?? 'active' }}</span></td><td><a class="admin-btn admin-btn-secondary" href="{{ route('admin.products.show',$product) }}">View</a></td></tr>@empty<tr><td colspan="7"><div class="admin-empty"><i class="fas fa-cubes"></i>No products match your filters.</div></td></tr>@endforelse</tbody></table></div><div class="admin-pagination">{{ $products->links() }}</div></div>
@endsection

@extends('layouts.admin')
@section('title','Product Details')
@section('content')
<div class="admin-page-header"><div><h1 class="admin-page-title"><i class="fas fa-cube"></i>{{ $product->name }}</h1><p class="admin-page-description">Catalogue record #{{ $product->id }}</p></div><a class="admin-btn admin-btn-primary" href="{{ route('admin.products.edit',$product) }}"><i class="fas fa-pen"></i>Edit product</a></div>
<div class="admin-card" style="display:grid;grid-template-columns:minmax(180px,280px) 1fr;gap:24px"><div style="background:#0b1627;border-radius:15px;display:grid;place-items:center;min-height:210px;overflow:hidden">@if($product->image)<img src="{{ asset('products/'.$product->image) }}" alt="" style="width:100%;height:100%;object-fit:cover">@else<i class="fas fa-image" style="font-size:40px;color:var(--admin-muted)"></i>@endif</div><div><div class="admin-stat-grid"><div class="admin-stat"><div class="admin-stat-label">Price</div><div class="admin-stat-value">₹{{ number_format($product->price,2) }}</div></div><div class="admin-stat"><div class="admin-stat-label">Stock</div><div class="admin-stat-value">{{ $product->stock }}</div></div><div class="admin-stat"><div class="admin-stat-label">Status</div><div class="admin-stat-value" style="font-size:15px">{{ ucfirst($product->status ?? 'active') }}</div></div></div><p style="color:var(--admin-muted);line-height:1.7">{{ $product->description ?: 'No product description has been provided.' }}</p><p style="margin-top:16px"><span class="admin-badge admin-badge-info">{{ $product->category }}</span> <span class="admin-badge admin-badge-neutral">{{ $product->brand ?: 'Unbranded' }}</span></p><p style="margin-top:16px;color:var(--admin-muted)">Seller: {{ $product->seller?->business_name ?: $product->seller?->name ?: 'Platform catalogue' }}</p></div></div>
@endsection

@extends('layouts.admin')
@section('title','Transactions')
@section('content')
<div class="admin-page-header"><div><h1 class="admin-page-title"><i class="fas fa-money-check-dollar"></i>Transactions</h1><p class="admin-page-description">Live gateway transaction records only.</p></div></div><form class="admin-toolbar" method="GET"><input class="admin-input" name="search" value="{{ request('search') }}" placeholder="Order or payment ID"><select class="admin-select" name="status"><option value="">All statuses</option>@foreach(['pending','processing','paid','verified','failed'] as $status)<option value="{{ $status }}" @selected(request('status')===$status)>{{ ucfirst($status) }}</option>@endforeach</select><button class="admin-btn admin-btn-primary">Filter</button></form><div class="admin-card"><div class="admin-table-wrap"><table class="admin-table"><thead><tr><th>Transaction</th><th>Customer</th><th>Amount</th><th>Method</th><th>Status</th><th>Date</th></tr></thead><tbody>@forelse($transactions as $transaction)<tr><td>#{{ $transaction->id }}<br><small>{{ $transaction->gateway_payment_id ?: $transaction->gateway_order_id ?: 'Gateway reference pending' }}</small></td><td>{{ $transaction->user?->name ?? 'Guest / removed user' }}</td><td>₹{{ number_format((float)($transaction->amount ?? (($transaction->amount_paise ?? 0)/100)),2) }}</td><td>{{ ucfirst($transaction->payment_method) }}</td><td><span class="admin-badge admin-badge-{{ in_array($transaction->status,['paid','verified'])?'success':($transaction->status==='failed'?'danger':'warning') }}">{{ $transaction->status }}</span></td><td>{{ $transaction->created_at?->format('d M Y, h:i A') }}</td></tr>@empty<tr><td colspan="6"><div class="admin-empty"><i class="fas fa-credit-card"></i>No payment transactions match this view.</div></td></tr>@endforelse</tbody></table></div><div class="admin-pagination">{{ $transactions->links() }}</div></div>
@endsection

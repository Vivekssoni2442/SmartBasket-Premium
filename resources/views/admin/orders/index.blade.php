@extends('layouts.admin')
@section('title','Orders')
@section('content')
<div class="admin-page-header"><div><h1 class="admin-page-title"><i class="fas fa-bag-shopping"></i>Orders</h1><p class="admin-page-description">Platform order monitoring without altering customer order actions.</p></div></div><form class="admin-toolbar" method="GET"><input class="admin-input" name="search" value="{{ request('search') }}" placeholder="Order ID"><select class="admin-select" name="order_status"><option value="">Order status</option>@foreach(['pending','completed','cancelled'] as $status)<option value="{{ $status }}" @selected(request('order_status')===$status)>{{ ucfirst($status) }}</option>@endforeach</select><select class="admin-select" name="payment_status"><option value="">Payment status</option>@foreach(['pending','paid','success','failed'] as $status)<option value="{{ $status }}" @selected(request('payment_status')===$status)>{{ ucfirst($status) }}</option>@endforeach</select><button class="admin-btn admin-btn-primary">Filter</button></form><div class="admin-card"><div class="admin-table-wrap"><table class="admin-table"><thead><tr><th>Order</th><th>Customer</th><th>Amount</th><th>Payment</th><th>Order state</th><th>Date</th><th></th></tr></thead><tbody>@forelse($orders as $order)<tr><td>#{{ $order->id }}</td><td>{{ $order->user?->name ?? $order->name }}</td><td>₹{{ number_format((float)($order->amount ?? $order->total ?? 0),2) }}</td><td><span class="admin-badge admin-badge-{{ in_array(strtolower($order->payment_status ?? ''),['paid','success','verified'])?'success':'warning' }}">{{ $order->payment_status ?? 'pending' }}</span></td><td><span class="admin-badge admin-badge-{{ str_contains(strtolower($order->order_status ?? $order->status ?? ''),'cancel')?'danger':'info' }}">{{ $order->order_status ?? $order->status ?? 'pending' }}</span></td><td>{{ $order->created_at?->format('d M Y') }}</td><td><a class="admin-btn admin-btn-secondary" href="{{ route('admin.orders.show',$order) }}">View</a></td></tr>@empty<tr><td colspan="7"><div class="admin-empty"><i class="fas fa-bag-shopping"></i>No orders match your filters.</div></td></tr>@endforelse</tbody></table></div><div class="admin-pagination">{{ $orders->links() }}</div></div>
@endsection

@extends('layouts.admin')
@section('title','Revenue')
@section('content')
<div class="admin-page-header"><div><h1 class="admin-page-title"><i class="fas fa-chart-line"></i>Revenue</h1><p class="admin-page-description">Financial totals are computed from verified and paid payment records.</p></div><a class="admin-btn admin-btn-secondary" href="{{ route('admin.transactions.index') }}">View transactions</a></div><div class="admin-stat-grid"><div class="admin-card admin-stat"><div class="admin-stat-label">Collected revenue</div><div class="admin-stat-value">₹{{ number_format($totalRevenue,2) }}</div></div><div class="admin-card admin-stat"><div class="admin-stat-label">Successful payments</div><div class="admin-stat-value">{{ $successfulPayments }}</div></div><div class="admin-card admin-stat"><div class="admin-stat-label">Pending amount</div><div class="admin-stat-value">₹{{ number_format($pendingRevenue,2) }}</div></div><div class="admin-card admin-stat"><div class="admin-stat-label">Failed amount</div><div class="admin-stat-value">₹{{ number_format($failedRevenue,2) }}</div></div></div><div class="admin-card"><div class="admin-card-title">Recent payment activity</div><div class="admin-table-wrap"><table class="admin-table"><thead><tr><th>Transaction</th><th>Customer</th><th>Amount</th><th>Status</th><th>Created</th></tr></thead><tbody>@forelse($recentTransactions as $transaction)<tr><td>#{{ $transaction->id }}</td><td>{{ $transaction->user?->name ?? 'Guest / removed user' }}</td><td>₹{{ number_format((float)($transaction->amount ?? ($transaction->amount_paise/100)),2) }}</td><td>{{ ucfirst($transaction->status) }}</td><td>{{ $transaction->created_at?->format('d M Y') }}</td></tr>@empty<tr><td colspan="5">No payment data has been recorded.</td></tr>@endforelse</tbody></table></div></div>
@endsection

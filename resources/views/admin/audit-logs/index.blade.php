@extends('layouts.admin')
@section('title','Audit Logs')
@section('content')
<div class="admin-page-header"><div><h1 class="admin-page-title"><i class="fas fa-shield-halved"></i>Audit Logs</h1><p class="admin-page-description">Security-relevant administrator actions. Sensitive credentials are never recorded.</p></div></div><form class="admin-toolbar" method="GET"><input class="admin-input" name="action" value="{{ request('action') }}" placeholder="Action, e.g. product_update"><input class="admin-input" type="date" name="date_from" value="{{ request('date_from') }}"><input class="admin-input" type="date" name="date_to" value="{{ request('date_to') }}"><button class="admin-btn admin-btn-primary">Filter</button></form><div class="admin-card"><div class="admin-table-wrap"><table class="admin-table"><thead><tr><th>Admin</th><th>Action</th><th>Target</th><th>IP</th><th>Details</th><th>Timestamp</th></tr></thead><tbody>@forelse($logs as $log)<tr><td>{{ $log->admin?->name ?? 'System / former admin' }}</td><td><span class="admin-badge admin-badge-{{ $log->status === 'failure' ? 'danger':'info' }}">{{ str_replace('_',' ',$log->action) }}</span></td><td>{{ $log->entity_type ? $log->entity_type.' #'.$log->entity_id : '—' }}</td><td>{{ $log->ip_address ?: '—' }}</td><td>{{ $log->description }}</td><td>{{ $log->created_at?->format('d M Y, h:i A') }}</td></tr>@empty<tr><td colspan="6"><div class="admin-empty"><i class="fas fa-shield-halved"></i>No audit events match these filters.</div></td></tr>@endforelse</tbody></table></div><div class="admin-pagination">{{ $logs->links() }}</div></div>
@endsection

@extends('layouts.admin')
@section('title','Returns & Cancellations')
@section('content')
<div class="admin-page-header"><div><h1 class="admin-page-title"><i class="fas fa-arrow-rotate-left"></i>Returns & Cancellations</h1><p class="admin-page-description">The current order system records cancellation reasons; those records are listed here.</p></div></div><div class="admin-card"><div class="admin-table-wrap"><table class="admin-table"><thead><tr><th>Order</th><th>Customer</th><th>Reason</th><th>Amount</th><th>Date</th></tr></thead><tbody>@forelse($returns as $return)<tr><td><a class="admin-btn admin-btn-secondary" href="{{ route('admin.orders.show',$return) }}">#{{ $return->id }}</a></td><td>{{ $return->user?->name ?? $return->name }}</td><td>{{ $return->cancellation_reason }}</td><td>₹{{ number_format((float)($return->amount ?? $return->total ?? 0),2) }}</td><td>{{ ($return->cancelled_at ?? $return->created_at)?->format('d M Y') }}</td></tr>@empty<tr><td colspan="5"><div class="admin-empty"><i class="fas fa-arrow-rotate-left"></i>No cancellation or return records are available.</div></td></tr>@endforelse</tbody></table></div><div class="admin-pagination">{{ $returns->links() }}</div></div>
@endsection

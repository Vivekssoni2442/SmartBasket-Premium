@extends('layouts.admin')
@section('title','Customer Activity')
@section('content')
<div class="admin-page-header"><div><h1 class="admin-page-title"><i class="fas fa-wave-square"></i>Customer Activity</h1><p class="admin-page-description">Order events are the customer activity currently recorded by SmartBasket.</p></div></div>
<div class="admin-card"><div class="admin-table-wrap"><table class="admin-table"><thead><tr><th>Customer</th><th>Activity</th><th>Related order</th><th>Status</th><th>When</th></tr></thead><tbody>@forelse($activities as $order)<tr><td>{{ $order->user?->name ?? 'Guest / removed user' }}<br><small>{{ $order->user?->email }}</small></td><td>Placed an order for ₹{{ number_format((float)($order->amount ?? $order->total ?? 0),2) }}</td><td><a class="admin-btn admin-btn-secondary" href="{{ route('admin.orders.show',$order) }}">#{{ $order->id }}</a></td><td><span class="admin-badge admin-badge-{{ in_array(strtolower($order->payment_status ?? ''),['paid','success','verified']) ? 'success':'warning' }}">{{ $order->payment_status ?? $order->order_status ?? $order->status ?? 'Pending' }}</span></td><td>{{ $order->created_at?->format('d M Y, h:i A') }}</td></tr>@empty<tr><td colspan="5"><div class="admin-empty"><i class="fas fa-stream"></i>No recorded customer order activity yet.</div></td></tr>@endforelse</tbody></table></div><div class="admin-pagination">{{ $activities->links() }}</div></div>
@endsection

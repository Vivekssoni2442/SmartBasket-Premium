<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Payment #SB-{{ $order->id }}</title><link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"><link rel="stylesheet" href="{{ asset('css/premium-dark-theme.css') }}"></head><body class="bg-dark text-white"><main class="container py-4" style="max-width:720px"><a class="btn btn-outline-light btn-sm mb-3" href="{{ route('seller.payments.index') }}">← Payment History</a><section class="card bg-black border-secondary p-4"><p class="text-secondary mb-1">Order #SB-{{ $order->id }}</p><h1 class="h3 text-success">₹{{ number_format($order->total, 2) }}</h1><p><span class="badge bg-secondary">{{ $paymentStatus }}</span> {{ $order->payment_method ?: 'Not recorded' }} · {{ $order->created_at?->format('d M Y, h:i A') }}</p><hr><h2 class="h6">Customer</h2><p>{{ $customer?->name ?: $order->name }}<br><span class="text-info">Customer UID: {{ $customer?->customer_uid ?: 'Guest checkout — no customer UID' }}</span></p><h2 class="h6">Products</h2>@foreach($sellerItems as $item)@php($product = $products[$item['product_id'] ?? null] ?? null)<div class="d-flex justify-content-between align-items-center border-top border-secondary py-2">@if($product?->image)<img src="{{ asset('products/'.$product->image) }}" alt="" width="42" height="42" class="rounded me-2" style="object-fit:cover">@endif<span class="me-auto">{{ $item['name'] ?? $product?->name ?? 'Product' }} × {{ $item['quantity'] ?? 1 }}</span><span>₹{{ number_format(($item['price'] ?? 0) * ($item['quantity'] ?? 1), 2) }}</span></div>@endforeach<hr><small class="text-secondary">Seller: {{ $seller->shop_name ?: $seller->seller_name }} · Transaction reference: not available in the current payment schema.</small></section></main></body></html>

<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Payment History</title><link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"><link rel="stylesheet" href="{{ asset('css/premium-dark-theme.css') }}"></head><body class="bg-dark text-white"><main class="container py-4" style="max-width:1000px"><div class="d-flex justify-content-between align-items-center mb-4"><div><h1 class="h3 mb-1">Payment History</h1><p class="text-secondary mb-0">Customer payments for {{ $seller->shop_name ?: $seller->seller_name }}</p></div><a href="{{ route('seller.settings') }}" class="btn btn-outline-light">Settings</a></div><div class="card bg-black border-success p-3 mb-3"><small class="text-secondary">Total Received</small><strong class="fs-3 text-success">₹{{ number_format($summary['received'], 2) }}</strong></div><form class="card bg-black border-secondary p-3 mb-3"><div class="row g-2"><div class="col-md-4"><input class="form-control" name="search" value="{{ request('search') }}" placeholder="Customer, UID, order or product"></div><div class="col-md-2"><select class="form-select" name="status"><option value="">All statuses</option>@foreach(['Successful','Pending','Failed','Refunded'] as $value)<option @selected(request('status')===$value)>{{ $value }}</option>@endforeach</select></div><div class="col-md-2"><select class="form-select" name="method"><option value="">All methods</option>@foreach(['COD','UPI','Card','Google Pay','PhonePe','Net Banking'] as $value)<option @selected(request('method')===$value)>{{ $value }}</option>@endforeach</select></div><div class="col-md-2"><input class="form-control" type="date" name="from" value="{{ request('from') }}"></div><div class="col-md-2"><button class="btn btn-primary w-100">Filter</button></div></div></form><div class="card bg-black border-secondary">@forelse($orders as $order)@php($status = app(\App\Http\Controllers\SellerPaymentController::class)->status($order))@php($customer = $order->user)<div class="border-bottom border-secondary p-3"><div class="d-flex justify-content-between gap-3"><div><strong>👤 {{ $customer?->name ?: $order->name }}</strong><div class="small text-info">Customer UID: {{ $customer?->customer_uid ?: 'Guest checkout — no customer UID' }}</div><div class="small text-secondary">Order #SB-{{ $order->id }} · {{ $order->created_at?->format('d M Y · h:i A') }}</div></div><div class="text-end"><strong class="text-success">₹{{ number_format($order->total, 2) }}</strong><div><span class="badge {{ $status === 'Successful' ? 'bg-success' : ($status === 'Pending' ? 'bg-warning text-dark' : 'bg-danger') }}">{{ $status }}</span></div></div></div><div class="mt-2">@forelse($order->seller_items as $item)@php($product = $order->seller_products[$item['product_id'] ?? null] ?? null)<div class="d-flex align-items-center gap-2 small mb-1">@if($product?->image)<img src="{{ asset('products/'.$product->image) }}" alt="" width="36" height="36" class="rounded" style="object-fit:cover">@endif<span>{{ $item['name'] ?? $product?->name ?? 'Product' }} × {{ $item['quantity'] ?? 1 }}</span></div>@empty<span class="small text-secondary">Product details unavailable for this legacy order.</span>@endforelse</div><div class="small text-secondary mt-2">{{ $order->payment_method ?: 'Not recorded' }} · Transaction reference: not available in current records</div><a class="btn btn-outline-info btn-sm mt-2" href="{{ route('seller.payments.show', $order) }}">View Payment</a></div>@empty<div class="p-4 text-center text-secondary">No customer payment records match these filters.</div>@endforelse</div></main></body></html>
